export default function Loading() {
    return (
        <div style={{ padding: "2rem 0", textAlign: "center", color: "var(--muted-text)" }}>
            Loading today's news...
        </div>
    );
}
